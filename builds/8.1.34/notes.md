# PHP 8.1.34

Patch **8.1.34** (Dec 2025 security release). PHP 8.1 is EOL upstream; keep this line only while phpvm still supports it.

`craft.yml` pins the **8.1** minor line — StaticPHP resolves the latest 8.1.x source at build time. Record the exact patch built in the release notes and manifest (`php: "8.1.34"`).